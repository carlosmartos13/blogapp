

<div class="card mt-4">
    <div class="card-body">
        
        
        
        <h1>{{postagem.titulo}}</h1>
         <hr>
        <small> Data de publicação {{postagem.createdAt}}</small>
       
        <hr>
        <small>Categoria <strong>{{postagem.categoria.nome}}</strong></small>
       
         <p>{{postagem.conteudo}}</p>
    </div>
</div>
